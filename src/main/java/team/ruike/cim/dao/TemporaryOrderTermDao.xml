<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="team.ruike.cim.dao.TemporaryOrderTerm">
    <insert id="add" parameterType="temporaryOrderTerm">

    </insert>
    <update id="update" parameterType="temporaryOrderTerm">
        
    </update>
    <select id="select" resultMap="">

    </select>
    <select id="selectCount" resultType="int" parameterType="temporaryOrderTerm">

temporary_order_term_id
menu_id
menu_number
complete_number
temporary_order_term_state
product_batch
temporary_order_id
status

   /**
     * 临时订单项
     */
    private Integer temporaryOrderTermId;
    /**
     * 菜品外键
     */
    private Menu menu;
    private Integer menuNumber;
    private Integer completeNumber;
    private Integer temporaryOrderTermState;
    private String productBatch;
    private TemporaryOrder temporaryOrder;
    private Integer status;
    </select>
    <select id="selectById" parameterType="int" resultMap="">

    </select>
</mapper>
