<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="team.ruike.cim.dao.WarehouseDao">
    <insert id="add" parameterType="warehouse">

    </insert>
    <update id="update" parameterType="warehouse">

    </update>
    <resultMap id="warhouseMap" type="warehouse">
        <result column="warehouse_id" property="warehouseId"/>
    </resultMap>
    <select id="select" resultMap="warhouseMap">

    </select>
    <select id="selectCount" resultType="int" parameterType="warehouse">

    </select>
    <select id="selectById" parameterType="int" resultMap="">

    </select>
</mapper>
